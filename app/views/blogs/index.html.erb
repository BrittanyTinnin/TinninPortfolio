
<div class="container">
  <header class="blog-header py-3">
    <div class="row flex-nowrap justify-content-between align-items-center">
      <div class="col-12 text-center">
        <a class="text-dark display-4" href="#">A Coder's Blog</a>
      </div>
    </div>
    <hr>
  </header><!-- logo/header -->

  <div class="jumbotron p-4 p-md-5 text-white rounded bg-dark">
    <div class="col-md-6 px-0">
      <h1 class="display-4 font-italic"><%= @blogs.last.title %></h1>
      <p class="lead mb-0 text-white font-italic"><%= link_to "Continue reading...", blog_path(@blogs.last.id)%></p>
    </div>
  </div><!-- featured blog -->

  <div class="row">
    <div class="col-md-8 blog-main">
      <% @blogs.each do |blog| %>
        <div class="blog-post">
          <h2 class="blog-post-title"><%= blog.title %></h2>
          <p class="text-muted"><%= blog.created_at.strftime("%B %d, %Y")%></p>
          <p><%= truncate(blog.body, length: 50) %></p>
          <p class="lead mb-0 text-white font-italic"><%= link_to "Continue reading...", blog %></p>
          <hr>
        </div>
      <% end %>
    </div><!-- iteration of all blogs -->

    <aside class="col-md-4 blog-sidebar">
      <div class="p-4 mb-3 bg-light rounded">
        <h4 class="font-italic">Remember</h4>
        <p class="mb-0">Life is like riding a bicycle. To keep your balance you must keep moving.</p>
      </div>
      <div class="p-4">
        <h4 class="font-italic" >Topics</h4>
        <ol class="list-unstyled">
        <% @topics.each do |topic| %>
          <li><a href="#"><%= topic.title %></a></li>
        <% end %>
        <%= link_to "More..", topics_path %>
        </ol>
      </div>
      <div class="p-4">
        <h4 class="font-italic">Elsewhere</h4>
        <ol class="list-unstyled">
          <li><a href="https://medium.com/@brittanytinnin" target="_blank">Medium</a></li>
        </ol>
      </div>
    </aside><!-- info on the side -->
  </div>
  <%= link_to 'New Blog', new_blog_path %><!-- create new blog -->
</div>

